// @homeops/ui — shared React component library (empty shell, populated as needed).
// Export a placeholder so workspace resolution + type-checking pass until
// real components land here.
export const uiVersion = "0.0.1";
